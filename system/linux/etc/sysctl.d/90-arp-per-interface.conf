# Only answer ARP for addresses on the receiving interface, and announce the
# matching source address. Stops Wi-Fi answering for the ethernet address (and
# vice versa) when both are on the same subnet, which NetworkManager otherwise
# sees as an address conflict.
net.ipv4.conf.all.arp_ignore = 1
net.ipv4.conf.all.arp_announce = 2
